char *gnl(int fd)
{
	static char *s;
	char *line;

	int i = 0;

	s = [funcion para lectura, sacar la línea entera incluso con el sobrante]

	while s[i] && s[i] != '\n'
		i++;

	line = ft_substr(s, 0, i + 1);


	s = [funcion para sacar el contenido sobrante, copiar s, liberarlo y asignarle el resultado];

	return (line);
}